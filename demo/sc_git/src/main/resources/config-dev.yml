server:
  port: 3344
spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri:  https://github.com/degree1111/git_work.git
          search-paths:
            - git_work
      label: master
eureka:
  client:
    service-url:
      defaultZone:  http://localhost:7001/eureka


age: 22222
